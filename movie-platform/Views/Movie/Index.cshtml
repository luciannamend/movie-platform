@model IEnumerable<movie_platform.Models.Movie>
@{
    ViewData["Title"] = "Movies";
}

<h2>Movies</h2>

<!-- Filter & Sort Form -->
<form method="get" asp-controller="Movie" asp-action="Index">
    <div>
        <label for="genre">Filter by Genre:</label>
        <input type="text" name="genre" id="genre" value="@ViewData["CurrentGenre"]">
    </div>
    <div>
        <label for="minRating">Filter by Minimum Rating:</label>
        <input type="number" scale=0.1 min="0" max="10" name="minRating" id="minRating" value="@ViewData["CurrentMinRating"]">
    </div>
    <button type="submit">Apply Filters</button>
    <button type="button" onclick="clearFilters()">Clear Filters</button>
</form>

<!-- Movies Display -->
<table class="table">
    <thead>
        <tr>
            <th>Title</th>
            <th>Details</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var movie in Model)
        {
            var averageRating = movie.Ratings.Any() ? movie.Ratings.Average() : 0;

            <tr>
                <td>@movie.Title</td>
                <td><a asp-action="Details" asp-route-id="@movie.MovieId">See more</a></td>
            </tr>
        }
    </tbody>
</table>
<p>
    <a asp-action="Add">Add New Movie</a>
</p>
<script>
    function clearFilters() {
        document.getElementById("genre").value = '';
        document.getElementById("minRating").value = '';
        document.querySelector('form').submit();
    }
</script>
